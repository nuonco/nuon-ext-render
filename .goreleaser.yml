version: 2

builds:
  - id: nuon-ext-render
    main: .
    binary: nuon-ext-render
    ldflags:
      - -s -w -X github.com/nuonco/nuon-ext-render/cmd.BuildVersion={{ .Version }}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - id: nuon-ext-render
    builds:
      - nuon-ext-render
    format: tar.gz
    name_template: "nuon-ext-render-{{ .Os }}-{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
